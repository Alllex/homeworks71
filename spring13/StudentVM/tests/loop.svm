
        ldc 0;
        st 3;

        ldc 0;
        ldc 9;
        st 0;
        st 1;
loop:

        ld 0
        ld 1
        cmp
        br next
        jmp endloop

next:

        ld 3
        ldc 1
        add
        st 3

        ld 1
        ldc 1
        add
        st 1
        jmp loop

endloop:

        ld 3
        jmp exit

exit:

        hlt

