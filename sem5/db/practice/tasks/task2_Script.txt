Задание_02_Script
=================================================================================

1.
ПОСЛЕ ОДОБРЕНИЯ ПРЕПОДАВАТЕЛЕМ ВАШЕГО ПРЕДЫДУЩЕГО ЗАДАНИЯ  (ER-ДИАГРАММЫ) 
напишите скрипт создания схемы базы данных, соответствующей вашей диаграмме. В этом же скрипте должны 
быть и операторы заполнения таблиц тестовыми данными.

Пример скрипта - в файле "Пример скрипта.sql".

Предостережения, советы и требования:
   - для полей, по которым строятся ключи (PK и FK) рекомендую использовать тип данных integer 
   - типы связанных полей (PK и FK) должны совпадать
   - для начала следует избегать нестандартных типов данных (под стандартными имею 
     сейчас в виду integer, decimal, varchar, datetime)
   - тестовые данные д.б. более-менее осмысленными, 
     кол-во записей в каждой таблице - от 3 до 20 примерно
   - обратите внимание, что последовательность операторов удаления таблиц не может быть 
     случайной (не забудьте отладить и эту часть скрипта)

Временно сохраните скрипт в любом месте под любым именем в формате текстового файла (.txt или .sql).


2.
Создайте с помощью скрипта свою схему базы данных под MS SQL Server, тем самым отлаживая скрипт. 

Запуск программы-клиента:
   Start -> All Programs -> Microsoft SQL Server 2008 -> SQL Server Management Studio

Параметры соединения с сервером (Connect to Server):

   Server name: vdb.math.spbu.ru
   Authentification: SQL Server Authentification 
   Login: 371
   Pasword: Student.spbu.ru 

   За подробностями - к преподавателю. 


3.
Сдайте отлаженный скрипт: cохраните его в файле с именем 02_XX_script.sql (где XX - код вашей предметной области) 
в папке  \\MATHSRVFS\POM\<номер вашей группы>\<ваша фамилия>\ 

Подчеркиваю, что сданный скрипт должен быть отлаженным, так как в последующих заданиях я буду проверять 
ваши sql-запросы к базе иногда вне доступа к мат-меховским серверам, поэтому может потребоваться на ходу 
поднять базу из скрипта. 
