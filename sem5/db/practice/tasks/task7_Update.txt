«адание_07_Update
=================================================================================

ѕридумайте 5 операторов модификации данных, отладьте их в среде MS SQL Server 
и сохраните в текстовом файле (07_XX.sql), содержащем текстовые формулировки и операторы. 
(см. презентацию DataBase_07)

1.  ƒобавление в любую из таблиц новых записей с сохранением последовательной нумерации (с использованием max).

2.  »зменение данных в любой из таблиц без использовани€ подзапроса.

3.  »зменение данных в любой из таблиц с использованием подзапроса.

4. ”даление части записей из любой таблицы с использованием подзапроса.

5. ”даление записи и св€занных с ней, с предварительным пересозданием правила целостности "каскадное удаление". 
   (выберите такие "главную" и "зависимую" таблицы, чтобы на "зависимую" никто не ссылалс€, иначе придетс€ пересоздать
    более одного правила целостности). 
   Ќапример:
   -------------------------------------------------------
     5.  ”даление клиента и его заказов:

          ALTER TABLE order DROP CONSTRAINT fk_order_customer;
          ALTER TABLE order ADD CONSTRAINT fk_order_customer FOREIGN KEY (id_customer) REFERENCES customer(id_customer) ON DELETE CASCADE;  

          DELETE customer WHERE name = '—идоров'; 
   -------------------------------------------------------






